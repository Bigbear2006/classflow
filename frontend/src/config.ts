// let PORT = '';

export let BASE_URL = 'http://localhost:8000/api/v1/';

export const MAX_OWNED_ORGS_COUNT = 1;
//
// if (typeof window !== 'undefined') {
//   PORT = window.location.port ? ':8000' : '';
//   BASE_URL = `${window.location.protocol}//${window.location.hostname}${PORT}/api/v1/`;
// }
